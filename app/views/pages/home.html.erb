<h1 class="txt-head">View da Home</h1>
<div class="txt-items">
  <p class="txt-sentances">country: <%= @country %> </p>
  <p class="txt-sentances">city: <%= @city %> </p>
  <p class="txt-sentances">coordinates: <%= @coordinates %> </p>
</div>
<!-- < @spots.each do |spot| >
  <p>Address: <= spot.address ></p>
  <p>Crowd: <= spot.crowd_congestion ></p>
  <p>Description: <= spot.description ></p>
  <p>Latitude: <= spot.latitude ></p>
  <p>Longitude: <= spot.longitude ></p>
  <hr>
< end > -->
<div id="map"
      style="width: 100%; height: 600px;"
      data-markers="<%= @markers.to_json %>"
      data-mapbox-api-key="<%= ENV['MAPBOX_API_KEY'] %>">
</div>
<p id="demo"></p>
